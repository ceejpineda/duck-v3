<%= form_with(model: video_room) do |form| %>
  <% if video_room.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(video_room.errors.count, "error") %> prohibited this video_room from being saved:</h2>

      <ul>
        <% video_room.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :vonage_session_id, style: "display: block" %>
    <%= form.text_field :vonage_session_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
